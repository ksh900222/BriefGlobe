# translate — 영어 제목 한국어 번역 → `translations.json`

> ⏱️ **실행 순서: 번역(translate)은 Grok 작업(asia-news·brief·stock-news)보다 '먼저' 돈다.**
> GDELT 영어 기사는 수집 직후 즉시 번역해야 지연이 최소화된다. **Grok 기사는 Grok이 한국어로 직접 작성**하므로 번역이 필요 없다 → 느린 Grok 검색을 기다릴 이유가 전혀 없다. (auto_update.sh도 GDELT 수집 → **번역** → Grok 순서로 배치됨.)

> ✅ **너(Claude)가 곧 번역가다. API 키·외부 스크립트가 전혀 필요 없다.**
> `ANTHROPIC_API_KEY` 를 찾거나 요구하지 말고, `batch_translate.py`·`translate_batch.py` 같은
> 스크립트를 **실행하지 마라**(그건 옛 방식이라 무시). `to_translate.json` 의 각 `title` 을
> **네가 직접 그 자리에서 한국어로 번역**해 `translations.json` 에 써라. 곧바로 번역을 시작한다.
> ⚠️ 번역을 안 했으면서 "완료"라고 보고하지 마라 — 실제로 `translations.json` 에 쓴 것만 완료다.

> ⛔ **합성 제목은 번역하지 마라 (토큰 낭비 금지).**
> GDELT에서 실제 헤드라인을 못 긁은 사건은 과거 `"{행위자} · {사건유형}"` 형태로
> 자동 합성됐다(예: `British · 공격`, `Poland · 요구`, `France · 공개 성명`).
> 이런 항목은 내용이 빈약해 **지도·번역 모두에서 제외**하기로 했다.
> 이제 `merge_news.py` 가 이들을 **`to_translate.json` 생성 단계에서 자동으로 걸러내므로**
> 번역 대기 목록에 애초에 나타나지 않는다. 혹시라도 제목이
> `… · 공격 / · 요구 / · 협의 / · 호소 / · 양보 / · 공개 성명` 등
> **CAMEO 라벨로 끝나는 형태**가 보이면 그것은 합성 제목이니 **번역하지 말고 건너뛰어라.**
> (판정·필터는 `merge_news.py` 의 `is_synth_title()` · `SYNTH_ACTS` 참고.)

`to_translate.json` 에 **번역이 필요한 항목 전체**가 들어있다 (제목에 한글 없는 것들). 형식:

```json
[
  { "id": "84e030b9b24f",
    "title": "Trump Denies US Missile Attack on Iran School",
    "category_guess": "정치" }
]
```

## 🔴 가장 중요한 — 배치로 나눠 "끝까지 반복" 한다

⚠️ 한 번에 다 번역하려 하지 마라. 항목이 많으면(수십~수백) 출력이 도중에 잘려서
일부만 처리된다. 대신 **아래 루프를 `to_translate.json` 이 `[]` 가 될 때까지 반복**한다:

```
1. to_translate.json 을 읽는다.
2. 내용이 빈 배열 [] 이면 → 번역 완료. 루프 종료.
3. 앞에서부터 최대 40개만 골라 번역해 translations.json 에 쓴다 (이번 배치만, 덮어써도 됨).
4. `python3 merge_news.py` 를 실행한다.
   → 번역이 누적 저장되고, to_translate.json 이 줄어든다.
   → 출력의 "번역 대기 N건" 숫자가 0 이 될 때까지가 목표.
5. 1번으로 돌아가 반복한다.
```

- 매 배치는 `translations.json` 을 **덮어써도 된다** — `merge_news.py` 가 영구 누적하므로
  이전 번역은 **절대 사라지지 않는다.**
- **잡문(가십·여행후기·광고)도 빼지 말고 포함**하되 `relevance` 를 낮게(0~3) 줘라.
  ⚠️ 생략하면 제외가 아니라 "원문 그대로 지도에 남는다." 빼는 유일한 방법은 낮은 점수로 포함.
- 한 배치 안의 **모든 id** 를 빠짐없이 처리한다 (그 40개는 전부).

## 출력 형식 — `translations.json`

```json
[
  {
    "id": "84e030b9b24f",
    "title": "트럼프, 이란 학교 미사일 공격 부인",
    "summary": "트럼프가 미국의 이란 학교 공습 의혹을 부인했다.",
    "category": "분쟁",
    "relevance": 8
  }
]
```

### 필드 규칙
- **id**: `to_translate.json` 의 값을 **그대로** 복사 (절대 변경 금지 — 원본 연결 키).
- **title**: 자연스러운 한국어 의역.
- **summary**: 한국어 요약 **2줄 정도(2문장, 약 40~80자)**. 무슨 일인지 + 배경/의미가 드러나게. (제목 반복 금지)
- **category**: 위 공통 6종 중 올바른 것. `category_guess`가 틀렸으면 **바로잡는다**
  (예: 지진 기사가 '정치'면 → '사회').
- **relevance** (0~10): 그 사건의 중요도. **상단 "공통 — 중요도 채점 기준" 표를 따를 것.**
  (예: "Youth Pastor Charged For Killing Wife" → 2, "13 Hours on Korean Air" → 1)
  - 헷갈리면 5를 준다 (제외 안 됨). **확실한 잡문에만 0~3.**
- ⭐ **인물·표기 앵커(2026 기준)**: 대한민국 대통령 = **이재명**(2025.6~). 외래어 인명은 표준 표기(예: Kushner → **쿠슈너**). 원문이 현직 직책을 틀리게 말한 명백한 오류는 바로잡는다('이준석 대통령' 등 금지).

---
