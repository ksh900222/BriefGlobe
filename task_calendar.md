# calendar — 거시·실적 발표 캘린더 → `econ-calendar.json`

**▶ 큐 기반 절차:** ① 향후 2~4주 예정 발표 + **최근 3일 발표분(actual)** 을 조사한다. ② 캘린더의 **모든** 거시·실적 이벤트에 대해 **신규 발표일·컨센서스(forecast)·실제값(actual)** 을 확인해 `econ-calendar.json` 에 기록(rolling window). ③ 이 캘린더가 E-2/G 큐의 원천이므로 **어떤 발표도 빠뜨리지 않는다**. 저장 후 `merge_econ.py` 가 누적(store)한다.

> 🕐 **갱신 주기: 하루 1회.** 발표 일정은 자주 안 바뀌므로 매일 한 번이면 충분.
> ♻️ 네가 만드는 `econ-calendar.json` 은 rolling window 그대로 두면 된다 — 별도 `merge_econ.py` 가
>   이걸 `econ-calendar-store.json` 에 **누적 병합**해 과거 발표(특히 `actual`)를 1년 보존한다(로직 변경 불필요).

미·한·일·중의 **예정된 거시지표 발표**와 **주요 종목 실적발표** 일정을 모아 `econ-calendar.json` 에 저장.
**공식 캘린더 출처가 있는 일정만**(지어내기 금지, 추정이면 명시). 대상: 향후 2~4주 + **최근 3일 발표분(actual 채우기용)**.
- **거시**: 미(FOMC·CPI·Core CPI·PCE·PPI·고용·소매판매·주택), 한(한은 기준금리·CPI·수출입), 일(BOJ·CPI), 중(PBOC LPR·CPI·PPI·소매판매·산업생산), **유럽(ECB 금리·유로존 HICP·PMI, 영국 BoE·CPI)**
- **실적**: `market-data.js`의 **주식 그룹(미국·한국) 전 종목**(현재 미국 19 + 한국 11)의 다음 실적발표일. 종목 수를 하드코딩하지 말고 두 그룹 티커 전체를 대상으로 한다(신규 종목 자동 포함).

  · 🛡️ **아래는 `market_guard.py` 가 기계적으로 차단한다 — 어기면 그 항목은 폐기된다.**
    1. **유니버스 밖 티커 금지.** `market-data.js` 주식 그룹에 없는 종목을 만들지 마라
       (012330·051910·066570·017670 을 임의 추가했다가 전량 폐기된 전례).
    2. **티커↔회사명 일치.** `market-data.js` 의 `name` 을 그대로 쓴다. 헷갈리는 것:
       `034020.KS`=**두산에너빌리티**(현대글로비스 아님) · `011070.KS`=**LG이노텍**(LG디스플레이·LG화학 아님) ·
       `003550.KS`=**LG 지주**(LG화학 아님) · `000720.KS`=**현대건설**(현대증권 아님) · `XE`=**엑스에너지**(Xerox 아님).
    3. **발표일 = 분기말 + 60일 이내**이고, **그 분기 공시가 이미 있으면 미래 '예정'일 수 없다.**
       한국 2분기 실적은 7월 말 발표다. 확정일을 못 찾으면 그 종목은 넣지 마라.
       ⛔ 여러 종목을 하루씩 밀어 **연속 배열**하는 건 창작이다(8/19·8/20·8/21… 전량 폐기된 전례).
    4. **한 티커·한 분기에 이벤트는 하나.** 이미 있으면 새로 만들지 말고 그 줄을 갱신한다.
    5. **산술 정합성.** `영업이익 > 매출`·`순이익 > 매출` 은 불가능. 단위(조/억)를 섞지 마라.
       ⚠️ 반대로 마진이 높다고 임의로 낮추지도 마라 — SK하이닉스 2026Q2 영업이익률은 실제 76%(공시)다.
  · ⚠️ **발표일 정확성 — 미래 분기 오배치 금지**: **회계분기 종료일 ≠ 발표일**이다. 실적은 분기가 **끝난 뒤 3~6주** 지나 발표된다(예: **9월말 분기 → 10월말~11월 초** 발표). **아직 끝나지도 않은 미래 분기(종료월이 오늘보다 뒤)를 현재·과거 날짜에 당겨 넣지 마라.** 확정 발표일을 공식 출처(회사 IR·나스닥/거래소 실적 캘린더)로 못 찾으면 **그 종목은 이번에 넣지 마라** — 추정 날짜를 창작하지 말 것.
  · ⛔ **가이던스·전망(guidance)은 실적 '발표' 이벤트가 아니다.** `kind:"실적"`으로 넣지 마라. 실제 발표일이 있고 그날 `actual`(실제값)이 나오는 것만 실적 이벤트다.
- **시장(구조일)** `kind:"시장"`: 증시 수급·변동성에 영향 주는 구조적 일정.
  · **네 마녀의 날·옵션/선물 만기**(미 셋째 금요일, 한 둘째 목요일)는 `market_dates.py`가 **규칙으로 자동 계산·삽입**한다 →
    **너(AI)는 이 만기일을 직접 생성하지 마라**(날짜 오산·중복 방지).
  · 대신 **비정형 시장 이벤트는 발견 시 `kind:"시장"`으로 추가**하라 — 지수 리밸런싱(MSCI·FTSE·S&P 정기변경),
    배당락일(대형 지수·주요 종목), 대규모 IPO·보호예수(락업) 해제, 대형 국채 입찰, 증시 휴장일 등. **공식 일정만**(지어내기 금지).

**⭐ `forecast`(컨센서스)를 반드시 구체적 숫자로 채운다 — 이게 이 작업의 핵심:**
- **거시지표**: 증권사·시장 **컨센서스 값**(예 CPI `"2.7%"`, 고용 `"+18.5만"`, 기준금리 `"동결(5.25%)"`). Trading Economics·Investing 등 출처.
  - **⭐ 물가·소비 지표는 MoM(전월비)과 YoY(전년비)를 함께 기입한다** — CPI·Core CPI·PCE·Core PCE·PPI·소매판매·개인소비 등은 `prev`·`forecast`·`actual` 모두 `"YoY +2.7% · MoM +0.3%"` 형식으로(두 값이 다 공표될 때). MoM만/YoY만 나오는 지표는 있는 것만. 한국·유로존 등 해외 물가도 동일. (한 값만 넣던 기존과 달리 두 값 병기 — 예: `"forecast": "YoY +2.7% · MoM +0.3%"`)
- **실적**: **EPS·매출 컨센서스**를 `forecast`에 (예 `"EPS $2.35 · 매출 $94.5B"`). stockanalysis·Zacks·Yahoo 등.
- **금리결정·선거 등 확률형 이벤트**: 가능하면 **폴리마켓(Polymarket) 컨센서스**를 함께 (예 `"동결 92% (Polymarket)"`). 출처 명시.
- 컨센서스를 못 찾으면 `forecast`는 `null`(지어내기 금지). "확인 필요" 같은 모호한 값은 넣지 말 것.

**⭐ 발표된 지표는 `actual`을 채운다:** 최근 3일 내 발표분은 실제값을 `actual`에 넣어(예 `"2.9%"`, `"EPS $2.41"`) 컨센서스/실제 대비가 보이게 한다. 미발표는 `actual: null`.

**출력 — `econ-calendar.json`** (JSON 배열, 날짜 오름차순):
```json
[
  { "date": "2026-07-30", "time": "21:30", "country": "미국", "kind": "거시",
    "title": "6월 PCE 물가", "importance": 3, "prev": "2.6%", "forecast": "2.7%", "actual": null,
    "source": "https://공식일정링크" },
  { "date": "2026-07-22", "country": "미국", "kind": "실적", "ticker": "TSLA",
    "title": "테슬라 2026 2분기 실적", "importance": 3,
    "forecast": "EPS $0.62 · 매출 $25.1B (컨센서스)", "actual": null,
    "source": "https://stockanalysis.com/stocks/tsla/" }
]
```
- `importance` 1~3(3=최상). `actual`=발표 전 `null`, 발표되면 실제값. `forecast`=컨센서스(구체 숫자), `prev`=이전값.
- ⏰ **`date`·`time`은 전부 한국시간(KST) 기준으로 변환해 기입한다.** `time`은 `"HH:MM"`(24시간) — 시간대 약어(ET/CST/CET/JST 등)를 붙이지 말고 **KST로 환산한 숫자만**.
  예: 미 08:30 ET → `"21:30"`, 유럽 14:00 CET → `"22:00"`, 중국 09:15 CST → `"10:15"`. 서머타임(미 EDT 등) 반영.
  시간대 변환으로 **날짜가 넘어가면 `date`도 KST 날짜**로. 미국 장마감 후 실적은 KST 기준 대략 `"05:00"`(익일)로 두거나 시각 미상이면 `time` 생략.

### E-2) 거시지표 시계열 누적 → delta JSON (거시 추세 차트, `macro-series.js`)

**▶ 큐 기반 절차:** ① `pipeline-todo.json` 의 **`macro_todo`**(각 `{seriesId, missingPeriods}`)와 **`unmapped`**(미매핑 = 신규 지표)를 읽는다.
② `macro_todo` 의 각 `seriesId`·`missingPeriods` 에 대해 그 회차의 실제값(act)·컨센서스(cons)를 조사한다.
③ `unmapped` 의 신규 지표는 `id·name·country·unit·match{any,not}` 를 정의해 신규 시리즈로 낸다(match 키워드는 실제 캘린더 제목에 맞춰).
④ 결과를 **`series-delta.json` 의 `macro` 섹션**에 담아 낸다(파일 직접 편집 금지). 저장은 `merge_series.py`.
- delta 형식: `{"macro": {"<id>": {"name","country","unit","match", "releases":[{"period","cons","act","momCons","momAct","source"}]}}}` — 기존 시리즈면 name/match 등 생략하고 `releases` 만 내도 됨(merge 가 메타 보존).
- **⭐ 물가·소비 지표(CPI·Core CPI·PCE·Core PCE·PPI·소매판매·수입물가 등)는 MoM(전월비)도 함께 낸다**: 각 회차에 `momCons`(MoM 컨센서스)·`momAct`(MoM 실제)를 추가. 기존 `cons`·`act`는 **YoY(전년비)** 값 그대로 유지. 예: `{"period":"2026-06","cons":3.7,"act":3.5,"momCons":0.3,"momAct":0.2,"source":"..."}`(YoY 3.5% · MoM +0.2%). MoM이 없는 지표(정책금리·PMI·심리지수 등)는 momCons/momAct 생략(null). 화면은 이 값으로 YoY 차트 밑에 **MoM 전용 차트**를 그린다.
> ♻️ **누적** — 과거 회차 보존(merge 가 append-only). 컨센서스 없는 지표는 `cons:null`(실제선만).

**구조 (JS 객체 리터럴 — eval 파싱):**
```js
const MACRO_SERIES = {
  "US_CPI": { "name":"미국 CPI (전년비)", "country":"미국", "unit":"%",
    "match": { "any":["CPI"], "not":[] },   // 캘린더 행 매핑(국가+제목 키워드). 기존 값 유지.
    "releases": [ { "period":"2026-06", "cons":3.8, "act":3.5, "source":"..." }, ... ] }
};
```
- ⚠️ **`period` = 참조월(reference month), `"YYYY-MM"` 고정.** 발표일이 아니다.
  7월 CPI 는 8월 12일에 발표돼도 `period` 는 **`"2026-07"`**. `"2026-08-12"` 같은 발표일이나
  `"2026-Q2"`·`"Q2 2026 (2026-06)"` 를 넣으면 **그 회차는 저장되지 않고 버려진다.**
- `cons`=발표시점 컨센서스(없으면 null), `act`=실제 발표값. `unit` 대개 `"%"`.
- ⛔ **`cons`·`act`·`momCons`·`momAct` 는 숫자만.** `"동결 3.50–3.75% (CME FedWatch)"` 같은 문장을
  넣으면 거부된다. 범위면 중간값을 숫자로, 설명은 `source` 에.
- 🚫 **아직 발표되지 않은 참조월에 `act` 를 넣지 마라 — 자동으로 지워진다.**
  · 참조월이 끝나기 전엔 실적이 존재할 수 없다(8월 지표는 8월이 끝나야 나온다).
  · 캘린더에 그 지표의 발표예정일이 미래로 잡혀 있으면 역시 발표 전이다.
  · **직전 달 값을 이번 달에 복사하는 것이 가장 흔한 사고다.** 실제로 7월 ISM 서비스 54.1 이
    8월 회차에 복제돼 '컨센서스 하회'로 그려졌고, 7월 CPI 3.4 도 8월에 복제됐다.
    발표 전이면 `cons` 만 넣고 `act` 는 **반드시 `null`**.
- 🔒 **시리즈 id 는 `macro_registry.json` 에 등록된 것만 쓴다 — 새 id 를 만들지 마라.**
  등록되지 않은 id 는 저장되지 않는다. 예전에 매 주기 새 이름을 지어내 한 지표가
  `US_ISM_MFG`·`US_ISM_MANUFACTURING`·`US_ISM_PMI`·`US_ISM_MANUF` 4개로 갈라졌고
  전체 시리즈가 130개까지 불어났다(정상 50개). 새 지표가 필요하면 레지스트리에 먼저 등록.
- **누적 규칙**: 매 주기 `econ-calendar`(calendar)의 각 거시 이벤트를 그 지표의 `MACRO_SERIES` 회차에 반영 —
  발표 전이면 `cons`(=`forecast`) 기록, 발표되면 같은 회차 `act`(=`actual`)를 채운다(그 회차 `cons`는 **보존**).
  **⭐ act를 채우는 회차는 cons도 반드시 함께**(그 회차 캘린더 `forecast`에서 컨센서스 수치 추출) — `act`만 넣고 `cons`를 null로 두지 마라. **컨센서스와 실제가 같아도 둘 다 기록**(그래프는 겹쳐도 둘 다 표시).
  **⭐ 물가·소비 지표는 MoM도 같은 규칙으로 매 발표마다 누적**: 발표 전이면 `momCons`(캘린더 `forecast`의 MoM 수치), 발표되면 같은 회차 `momAct`(캘린더 `actual`의 MoM 수치)를 채운다. 캘린더 값이 `"YoY +2.7% · MoM +0.3%"` 형식이므로 **YoY는 cons/act, MoM은 momCons/momAct로 분리 추출**한다. 즉 새 CPI·PCE·PPI 등이 발표될 때마다 그 회차의 YoY와 MoM이 **함께** 시리즈에 append되어 두 차트(YoY·MoM)가 계속 갱신된다.
  `match`(매핑 키워드)와 기존 `releases`는 **유지**, 새 회차만 append(중복 period 금지).
- **대상 지표**: 각국 **정책금리·CPI·근원물가·PCE·GDP·고용/PMI** 등 핵심(현재 17개 시리즈 유지·확장).
  새 지표가 필요하면 `id`+`name`+`country`+`unit`+`match`를 정의해 추가(제목 키워드는 실제 캘린더 제목에 맞춰).
- **지어내기 금지**: 실제값·컨센서스는 `econ-calendar`(calendar 산출) 또는 검증 가능한 출처(Trading Economics·공식통계·중앙은행)만. 불확실 = `null`. TTM/누적치와 혼동 금지.
- 저장: `macro-series.js` 를 위 구조로 덮어쓴다(전체 시리즈·회차 포함). 다른 파일은 건드리지 않는다.

---
