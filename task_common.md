# Grok 작업 지시서 — 동아시아 뉴스 수집 + 영어 뉴스 번역

너(Grok)는 이 프로젝트 폴더(`WEB_WORLD_INFO`)에서 작업한다.
세계지도에 위치별로 뉴스를 띄우는 프로젝트다. GDELT(영어권 위주)로 전 세계
backbone은 이미 채웠고, **너의 역할은 ①동아시아를 메우고 ②영어 제목을 번역하는 것**이다.

작업은 A, B 두 개다. **둘 다** 수행한다.

---

## ⭐ 공통 — 카테고리 6종 (두 작업 모두 이 정의를 쓴다)

반드시 아래 6개 중 **정확히 하나**를 고른다:

| 카테고리 | 포함 |
|----------|------|
| `분쟁` | 전쟁·공격·미사일·군사·안보·테러 |
| `정치` | 외교·정부·선거·국회·사법·정상회담 |
| `경제` | 산업·금융·시장·기업·무역·고용 |
| `사회` | 재난·사건사고·시위·범죄·노동·보건·질병 |
| `기술` | IT·인공지능·반도체·과학·우주 |
| `문화` | 문화·예술·연예·스포츠 |

## ⭐ 공통 — 중요도 채점 기준 (0~10 · asia-news `importance` / translate `relevance` / tech-deals `importance` 모두 이 눈금을 쓴다)

| 점수 | 기준 | 예시 |
|------|------|------|
| **9~10** | 세계 질서·시장을 흔드는 사건 | 개전·휴전, 주요국 정상 유고/급변, 금리·환율 서프라이즈, 사망 수백 명급 대형 재해 |
| **7~8** | 지역 정세·산업을 움직이는 사건 | 분쟁 격화/완화 전환점, 주요국 제재·핵심 정책 발표, 대기업 실적 쇼크·초대형 M&A, 광역 전염병 경보 |
| **5~6** | 통상적 중요 뉴스 | 정상회담·외교 성명, 예상 부합 경제지표, 중견 기업 뉴스 |
| **3~4** | 국지적 사건·후속 보도 | 지역 단위 사건, 이미 알려진 사안의 부연 |
| **0~2** | 연성 뉴스 | 연예·가십·스포츠 경기 결과·인물 동정, 여행후기·광고·시황표 |

**원칙**: "얼마나 화제가 될까"가 아니라 **"세계/지역 정세와 사람들의 삶에 실질 영향이 얼마나 큰가"**로 채점한다. 두 등급 사이에서 망설여지면 **낮은 쪽**을 택한다(점수 인플레 방지).

---

# 🔧 시장 파이프라인 공통 계약 (stock-fin·calendar·calendar-delta·earnings 공통 — 반드시 준수)

이 계약은 "커버리지 검증(coverage_check.py) → AI 조사(큐 기반) → 결정론 저장(merge_series.py) → 재검증" 흐름의 규칙이다.

1. **큐 항목만 조사**: `python3 coverage_check.py` 가 만든 **`pipeline-todo.json`** 의 항목만 처리한다.
   `{stocks_info_todo, stocks_news_todo, earnings_todo, earnings_stale, macro_todo[{seriesId,missingPeriods}], unmapped[]}`.
2. **파일 직접 편집 금지(series)**: `macro-series.js`·`earnings-series.js` 를 직접 고치지 말고, **표준 delta JSON**(`series-delta.json`)만 낸다 →
   저장은 `merge_series.py` 가 한다. (형식은 각 작업 참조.) `stocks-info.js`·`stocks-news.js`·`econ-calendar.json` 는 해당 작업이 직접 산출.
3. **검증된 사실만**: 실제 발표값·확인된 컨센서스만. **불확실 = `null`**. **완결성보다 검증 우선**(억지로 채우지 말 것).
4. **큐 전부 처리·로그**: 큐의 **모든 항목**을 처리하고, 못 한 건 이유를 남긴다(로그/주석). 어떤 항목도 조용히 건너뛰지 않는다.
5. **대상은 데이터 주도**: 조사 대상은 고정 목록이 아니라 **큐**다. 사용자가 SERIES·캘린더에 새 종목/지표를 넣으면 큐에 자동으로 뜬다 — 그것을 처리하라.
6. **금지**: 과거 회차/분기 삭제 · 임의 스키마 변경 · TTM(누적치)을 분기값으로 혼입 · 이 지시서(grok_task.md) self-edit · 지정된 산출물 외 대체 파일 생성.
- ✅ 완료 판정: 처리 후 `python3 coverage_check.py` 재실행 시 갭이 줄어야 한다.

---
